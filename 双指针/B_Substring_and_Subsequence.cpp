#include <bits/stdc++.h>
using namespace std;
#define endl '\n'
#define int long long

void solve(void){
    string s1,s2;
    cin >> s1 >> s2;
    int n = s1.size();
    int m = s2.size();
    int ans = m + n;
    for(int i = 0; i < n; i++){
        
    }

}

int main(void){
    ios::sync_with_stdio(0);
    cin.tie(0);
    cout.tie(0);
    int _;
    cin >> _;
    while(_--){
        solve();
    }
    return 0;
}
